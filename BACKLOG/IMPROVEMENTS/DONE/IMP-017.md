# IMP-017: tab-titles — ajouter tests automatisés basiques

**Type:** Improvement
**Statut:** A faire
**Priorite:** Basse
**Complexite:** S
**Tags:** tests
**Date creation:** 2026-03-21

---

## Description

tab-titles n'a aucun test automatisé. Découvert par le worker BUG-005+006 (boy scout).

## Tests suggérés

1. **Test _dot / palette** : exécuter _dot avec N noms de projets, vérifier que chaque résultat est dans la palette attendue et que "claude-conf" retourne ⚙️

2. **Test cohérence bash/zsh** : vérifier que les formules `% 6` (bash 0-indexed) et `% 6 + 1` (zsh 1-indexed) produisent les mêmes couleurs pour les mêmes inputs

3. **Test sed cross-platform** : sur un fichier temporaire avec `# DISABLE_AUTO_TITLE="true"`, vérifier que le uncomment fonctionne (tester les deux branches uname)

4. **Test syntaxe** : `bash -n` sur les deux fichiers

## Fichiers concernes

- `tab-titles/hooks/tab-title.sh` — à tester
- `tab-titles/install.sh` — à tester
- `tab-titles/tests/test-tab-titles.sh` — nouveau fichier de tests

## Criteres d'acceptation

- [ ] Les 4 tests ci-dessus existent et passent
- [ ] Les tests sont exécutables standalone
