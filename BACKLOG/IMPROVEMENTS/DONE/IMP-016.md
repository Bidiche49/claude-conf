# IMP-016: install.sh — ajouter tests automatisés basiques

**Type:** Improvement
**Statut:** A faire
**Priorite:** Basse
**Complexite:** S
**Tags:** tests
**Date creation:** 2026-03-21

---

## Description

install.sh n'a aucun test automatisé. Découvert par le worker BUG-004 (boy scout).

## Tests suggérés

1. **Test de régression arithmetic** : vérifier que le pattern `((var++))` n'est pas réintroduit
   ```bash
   grep -qE '\(\([a-z]+\+\+\)\)' install.sh && exit 1
   ```

2. **Test de survie au premier échec** : exécuter `bash install.sh --all` dans un sandbox (tmpdir avec un module factice qui fail) et vérifier que le script continue et affiche le résumé

3. **Test de syntaxe** : `bash -n install.sh`

## Fichiers concernes

- `install.sh` — le script à tester
- `tests/test-install.sh` — nouveau fichier de tests

## Criteres d'acceptation

- [ ] Au moins les 3 tests ci-dessus existent et passent
- [ ] Les tests sont exécutables standalone (`bash tests/test-install.sh`)
