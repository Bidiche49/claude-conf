# IMP-031: tab-titles hooks — ajouter set -e (convention projet)

**Type:** Improvement
**Statut:** A faire
**Priorite:** Basse
**Complexite:** XS
**Tags:** tab-titles, convention, set-e
**Depends on:** none
**Blocked by:** —
**Date creation:** 2026-04-08

---

## Description

Les hooks `tab-title.sh` et `session-tab-title.sh` ne commencent pas par `set -e`, en violation de la convention projet ("set -e en debut de chaque script"). Meme probleme que IMP-030 (supervisor-guard.sh) mais pour les hooks tab-titles.

## Fichiers concernes

- `tab-titles/hooks/tab-title.sh` — ajouter `set -e` apres le shebang
- `tab-titles/hooks/session-tab-title.sh` — ajouter `set -e` apres le shebang

## Criteres d'acceptation

- [ ] Les deux hooks commencent par `set -e`
- [ ] Les deux hooks fonctionnent correctement avec `set -e` (pas de faux echec sur grep/jq)
- [ ] Tests existants passent toujours

## Tests de validation

- [ ] `bash tab-titles/tests/test-tab-titles.sh` passe sans regression
- [ ] Test manuel : `ccs` → `/supervisor` → titre correct
